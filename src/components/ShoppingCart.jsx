/* import React from 'react';

const ShoppingCart = ({ cartItems }) => {
  return (
    <div>
      <h2>Cart</h2>
      <ul>
        {cartItems.map((item) => (
          <li key={item.id}>
            <p>{item.name}</p>
            <p>{item.quantity} x {item.price}</p>
          </li>
        ))}
      </ul>
      <p>Total: { Logic to calculate the amount }</p>
      { Button "Checkout" or other controls }
    </div>
  );
};

export default ShoppingCart; */
